# kotest-wasm

A sample Wasm project that shows how the `wasmJs` and `wasmWasi` targets can be tested by Kotest.

There is a shared function in commonMain which is tested in commonTest, and then each of the targets adds further logic.
